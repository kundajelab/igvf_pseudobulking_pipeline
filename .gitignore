__pycache__/
.nextflow/
.nextflow*
.venv/
.envrc

.vscode/
.zed/
# Claude Code: share project permission policy, keep personal overrides local.
# Must be `.claude/*` not `.claude/`; git will not descend into an excluded directory,
# so a negation under a trailing-slash exclusion never matches.
.claude/*
!.claude/settings.json
workspace/
.ruff_cache/
.DS_Store
.pixi/
build/
*.egg-info/
*.so
*.c

# files from igvf_utils
IU_Logs/
